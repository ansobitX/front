.custom-input {
  label {
    font-size: 11px;
    left: 2%;
  }

  .form-control {
    font-weight: 500;
    font-size: 12px;
    line-height: 12px;
    letter-spacing: 0.1px;
    color: var(--bright);
  }
}